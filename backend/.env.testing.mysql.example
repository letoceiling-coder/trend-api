# Copy to .env.testing.mysql and adjust. Do not commit .env.testing.mysql (in .gitignore).
# Used when running: php artisan migrate:fresh (with APP_ENV=testing and these DB_*).
# For PHPUnit MySQL tests, credentials are in phpunit.mysql.xml; override by exporting before run:
#   set -a; source .env.testing.mysql; set +a; php vendor/bin/phpunit -c phpunit.mysql.xml --testdox

APP_ENV=testing
APP_KEY=base64:2fl+Ktvkx2x+Fx1F2pTgwf3BPJQ5v+jknWy2oXkvRzE=

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=trend_api_test
DB_USERNAME=root
DB_PASSWORD=

CACHE_STORE=array
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
MAIL_MAILER=array
INTERNAL_API_KEY=test-internal-key
